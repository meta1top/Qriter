{
  "name": "@qriter/shared",
  "version": "0.0.1",
  "private": true,
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc --project tsconfig.json",
    "dev": "tsc --project tsconfig.json --watch",
    "clean": "rm -rf dist",
    "typecheck": "tsc --project tsconfig.json --noEmit",
    "test": "jest --config ../../jest.config.ts --roots libs/shared"
  },
  "dependencies": {
    "@qriter/types": "workspace:*",
    "zod": "^3"
  },
  "peerDependencies": {
    "@nestjs/common": "^11",
    "nestjs-zod": "^4",
    "reflect-metadata": "*"
  },
  "devDependencies": {
    "@types/node": "^22"
  }
}
